services:
  openai-tool-api:
    build: 
      context: .
      dockerfile: Dockerfile
    
    container_name: openai-tool-api

    ports:
      - "8000:8000"
    env_file:
      - .env

    restart: unless-stopped
